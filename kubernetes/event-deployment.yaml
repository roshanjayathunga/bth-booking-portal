apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: event-service
  template:
    metadata:
      labels:
        app: event-service
    spec:
      containers:
        - name: event-service
          image: roshanjayathunga/bth-booking-event-service:latest
          ports:
            - containerPort: 3002
          env:
            - name: PGUSER
              value: booking_user
            - name: PGPASSWORD
              value: POSTGRES_PASSWORD
            - name: PGDATABASE
              value: booking_db
            - name: PGHOST
              value: postgres
            - name: PGPORT
              value: "5432"
